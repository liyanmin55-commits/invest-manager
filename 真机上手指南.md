# 投资管家 · 真机上手指南

代码已经在你真机上了：`D:\lym12\WorkBuddy\投资管理系统\`
现在里面是**空库**（首次打开什么都没有），按下面几步把它变成你每天在用的工具。

---

## 第 1 步：在你机器上把站点跑起来

任选一种（站点本身是纯静态文件，不需要装任何依赖）：

**方式 A · 双击「启动投资管家.bat」（最省事，推荐）**
项目根目录有一个 `启动投资管家.bat`，双击它：
- 自动起 server.js 服务 + 自动打开浏览器到 `http://localhost:8080`
- **用完直接关掉那个黑窗口**，服务就停了（数据不丢，都存在浏览器里）
- 下次要用再双击即可

**方式 B · 手动在终端起服务（效果一样，适合想看日志的场景）**
项目根目录有一个 `server.js`（Node 内置 http 实现，无需装任何包、也不需要 Python）：
```
cd /d "D:\lym12\WorkBuddy\投资管理系统"
node server.js
```
看到一行 `投资管家已起服务: http://localhost:8080` 就成功了，浏览器开 `http://localhost:8080`。

> - **不要关闭那个命令行窗口**（关了就停了）；要停服务按 `Ctrl+C`
> - PowerShell / CMD / Git Bash 都能跑——`node` 是 `.exe`，不受 PowerShell 执行策略影响
> - 没装全局 Node？用 WorkBuddy 自带的：`"C:\Users\lym12\.workbuddy\binaries\node\versions\22.22.2\node.exe" server.js`

**方式 C · 双击 index.html**（应急用，不推荐）
直接双击 `index.html`。ECharts 走 CDN，所以要保持联网；且 `file://` 下行情代理和自动补名不可用，个别浏览器还会限制本地数据库。

**方式 D · 用 npx serve 起服务**（备选）
```
cd /d "D:\lym12\WorkBuddy\投资管理系统"
npx -y serve -l 8080
```
首次运行会临时下载 `serve` 包（要联网）。**PowerShell 里 `npx` 会被执行策略拦截**，请用 CMD 或 Git Bash 跑这一行。

> 推荐「方式 A 双击 bat」或方式 B（server.js）：零依赖、不受 PowerShell 拦、行情代理内置，本地数据库在 `http://` 下最稳定。

---

## 第 2 步：先看清界面（可选，看效果用）

打开后是空库，净值为 0。想先看完整效果，去「设置 → 危险区」点 **载入示例数据**，会写入一份演示持仓（茅台/ETF/基金/黄金/存款 + 一个定投计划）。看完去「设置 → 危险区」点 **清空全部数据** 即可归零。

---

## 第 3 步：录你自己的真实数据

> 小技巧：用方式 B（server.js）起服务后，记账/定投页输入代码**失焦会自动填入名称并拉到现价**；代理就在 server.js 里一起跑了，不用另外部署。

去「记账」页，每笔买卖记一条流水：
- **买**：账户（如"华泰"）、代码（如 `600519`）、名称、类型选"买"、数量、买入价、日期
- **卖**：类型选"卖"，填卖出数量和卖出价
- **存款/理财/现金**：代码填 `CASH`，类型"买"，数量填 1，价格填金额（如 20000）；或直接在数量填金额、价格填 1，二选一保持一致即可

录第一笔后系统会自动用"买入价"当现价兜底，净值立刻有数。之后想让市值更准：
- 手动：去「设置 → 手动行情」改每个标的的现价，点"保存行情"
- 自动：用 server.js 起服务后点顶部「刷新行情」即可（代理已内置，见下方"进阶 A"）

**定投自动执行（交易日判断 + 顺延）**：打开「投资管家」时，系统自动检查每个定投计划——到了执行日且是**交易日**就自动记一笔买入流水（账户=定投、方向=买入、金额=计划设定、价格取当前行情、份额=金额÷价格）；若执行日恰逢周末或法定节假日，则**顺延到下一个交易日**再记。手动「记录执行」按钮仍保留可用。
> 注意：① 系统只记账、**不真去券商下单**；② 计划首次建立当天不会自动记首笔（从下一周期起算），避免误记；③ 若该标的「刷新行情」里没有价，会自动跳过并在顶部提示"请先刷新行情"；④ 交易日历内置 **2025–2026** 法定节假日（含调休补班），**2027 年需届时补充**数据（改 `assets/tradingdays.js` 里的 `HOLIDAYS`/`EXTRA` 两个集合）。

**风险分层 + 配置体检**：记一笔 / 加定投时，表单多了「风险分层」下拉（**稳健 / 进阶 / 对冲**）。切换「资产类型」会自动预填——债券/存款/理财→稳健，股票/ETF/可转债/基金→进阶，黄金→对冲，**你随时可手动改**（基金内部差异大：短债基金请手动改成"稳健"）。去「分析」页顶部看**配置体检**：自动按你的持仓算出 ① 风险分层占比、② 最大单一持仓占比（>8% 红色提醒）、③ 权益类暴露（股票/ETF/可转债合计占比，>70% 提醒）、④ 定投日均现金流（评估每日/每周定投的现金压力）、⑤ 相关性提示（权益类≥2 只时提醒是否高度相关、分散有限）。全部用你本地数据算，不依赖联网。

---

## 第 4 步：立刻做第一次备份（重要）

去「设置 → 数据备份」点 **导出 JSON**，把文件存到网盘 / U盘 / 发邮件给自己。

⚠️ 数据只存在你本机浏览器里，清缓存、换设备、重装系统都会丢，且无法找回。建议**每录完一批就导出一次**，当成习惯。

---

## 进阶（按需要再做，不急）

**A. 自动刷新行情（已内置，零部署）**
行情代理已经直接合进 `server.js` 了——只要用「第 1 步 方式 B」起服务，浏览器从 `http://localhost:8080` 打开，代理就自动生效：
- 记账/定投页输入代码、**失焦自动补名称 + 拉现价**
- 顶部「刷新行情」一次性拉回全部持仓现价，净值自动重算

原理：`server.js` 在 Node 服务端（无 CORS 限制、国内直连东财/腾讯没问题）去抓行情，浏览器从同源 `http://localhost:8080/api` 拿数据。**不依赖 Cloudflare**——之前用的 `*.workers.dev` 在中国大陆直连不可达（须开 VPN），现已弃用。
- 若手动改过「设置 → 行情代理」填过 workers.dev 地址，重新打开会自动切回 `http://localhost:8080/api`
- 想换端口起服务（如 `node server.js 9090`），就把设置里的代理地址相应改成 `http://localhost:9090/api`

**B. 手机也能看**（同一 WiFi 局域网，零部署）
server.js 已支持手机访问，步骤：
1. 电脑上正常启动服务（双击 `启动投资管家.bat`），黑窗口里会打印一行 **`手机看(需同一WiFi): http://192.168.x.x:8080`**
2. 手机连**同一个 WiFi**，浏览器打开那个地址即可
3. 第一次启动如果 Windows 弹防火墙询问，勾选"专用网络"并点**允许**（错过弹窗的话，用管理员 CMD 跑：`netsh advfirewall firewall add rule name="InvestServer" dir=in action=allow protocol=TCP localport=8080`）

⚠️ **手机端是独立的空库**——数据存在各设备自己的浏览器里，不会自动同步。想让手机看到你的持仓：
- 电脑「设置 → 数据备份」**导出 JSON** → 通过微信/QQ 发到手机 → 手机打开页面后「设置 → 导入 JSON」
- 之后手机上也能「刷新行情」看实时盈亏（代理自动走电脑，无需配置）
- **主力记账建议固定在电脑**，手机适合"看"；两边都记账数据会分叉，要以一边为准再导出导入

> 只在家里 WiFi 用，别在公共 WiFi 下挂这个服务（局域网内任何设备都能访问该页面）。

---

## 进阶 C. 随时手机打开（云端版：GitHub Pages + Supabase）

进阶 B 是"同 WiFi 局域网"，离开家里就打不开。想要**任何网络、任何设备随时打开、且数据自动多端同步**，走云端版：

> 国内访问预期：GitHub Pages 与 Supabase 服务器都在海外，**国内访问比国内平台慢、偶尔抽风，但能开**（不像 workers.dev 那样直连被墙死）。数据离开本机存到云端，隐私权衡需接受。

**① 建 Supabase 云端库（存数据用，免费档够个人）**
1. 打开 supabase.com 注册，新建一个项目（记住 Project URL 和 anon public key，在 Settings → API 里）
2. 控制台 → SQL Editor → 新建查询 → 把本项目 `supabase/schema.sql` 全文粘贴进去执行（建 `transactions` / `plans` 两张表 + 行级安全）

**② 部署前端到 GitHub Pages（让手机任何网络能开）**
1. 在 GitHub 新建一个仓库（如 `invest-manager`），把本项目**全部文件**推上去（`.gitignore` 已排除 `.workbuddy/` 记忆目录，不会泄露）
2. 仓库 → Settings → Pages → Source 选 **GitHub Actions**
3. 推到 `main` 分支即自动部署（本项目已内置 `.github/workflows/deploy.yml`）；部署完成后访问 `https://<你的用户名>.github.io/<仓库名>/`
4. 注意：资源都是相对路径，`echarts.min.js` / `supabase.min.js` 已本地化进 `assets/vendor/`，**不依赖海外 CDN**

**③ 配置多端同步（手机/电脑看同一份数据）**
在任意设备打开页面 → 「设置 → 云端同步（可选）」填入：
- Supabase 项目 URL（https://xxxx.supabase.co）
- anon public key
- 云端库标识（自己编一段随机串，用于把你的数据和别人的隔离；建议用密码管理器生成）
点「保存并连接」，首次会把本地数据上传、之后自动双向同步（新增/修改/删除都会实时写云端）。

**④ 行情代理（GitHub Pages 上刷新行情需要公网代理）**
GitHub Pages 本身不带行情代理，需另配一个**国内可访问**的代理地址，在「设置 → 行情代理」填入：
- 推荐：把 `supabase/functions/quote/index.ts` 部署成 Supabase Edge Function（海外节点，国内可能慢；见该文件注释的部署步骤）
- 或：你有国内轻量服务器 / 腾讯云 SCF / 阿里云 FC，跑一份同样逻辑的代理
- 没配代理也能用：在「设置 → 手动行情」里手动维护现价

**⑤ 降级方案（兜底）**
云端不稳时，随时回到本地模式：不填云端同步 = 纯本机；电脑开 `启动投资管家.bat` + 手机同 WiFi 看（进阶 B）。数据可随时「设置 → 导出/导入 JSON」在设备间搬运。

---

## 检查点

- [ ] 站点能在你浏览器打开、六个页签都能点
- [ ] 已录入至少一笔真实持仓，总览有净值
- [ ] 已导出第一份 JSON 备份并存到别处
- [ ] （推荐）用 server.js 起服务后，输入代码失焦能自动补名称、顶部「刷新行情」能拉到现价

做完前三项，这套系统就真正属于你了。
